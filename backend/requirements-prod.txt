# Requirements para PRODUÇÃO (sem dependências de teste)
# Use este arquivo no Dockerfile para builds mais rápidos

# Core API
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database
sqlalchemy==2.0.36
psycopg2-binary>=2.9.0

# Validation
pydantic==2.10.0
pydantic-settings==2.6.0
pydantic[email]==2.10.0
email-validator==2.2.0

# Authentication
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
python-multipart==0.0.12

# Astronomy
ephem==4.1.5
kerykeion>=5.3.0
pytz>=2024.1
timezonefinder>=6.4.1

# Google OAuth
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0

# Groq API
groq>=0.4.1

# Utilities
numpy<2.0
PyPDF2==3.0.1
httpx>=0.27.0  # HTTP client (pode ser usado em alguns serviços)
resend>=2.0.0  # Email service (Resend)

# RAG Dependencies (consolidado no backend)
fastembed>=0.2.0

